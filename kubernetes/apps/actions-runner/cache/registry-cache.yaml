---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: registry
  namespace: actions-cache
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
  values:
    controllers:
      main:
        containers:
          main:
            image:
              repository: registry
              tag: 3.0.0@sha256:cd92709b4191c5779cd7215ccd695db6c54652e7a62843197e367427efb84d0e
            env:
              REGISTRY_STORAGE_FILESYSTEM_ROOTDIRECTORY: /data
              REGISTRY_PROXY_REMOTEURL: https://registry-1.docker.io
              # Default TTL is 7 days
              REGISTRY_DELETE_ENABLED: "true"
              # TODO: Docker hub creds
    service:
      main:
        ports:
          http:
            port: 5000
    persistence:
      data:
        enabled: true
        storageClass: ceph-block
        size: 100Gi
        accessMode: ReadWriteOnce
