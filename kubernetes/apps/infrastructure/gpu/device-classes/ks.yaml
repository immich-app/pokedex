apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: gpu-device-classes
spec:
  interval: 1h
  path: ./kubernetes/apps/infrastructure/gpu/device-classes/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  wait: true
